# Examples

This folder is a placeholder where you can add more datasets or scripts.

Ideas to try:
- Swap in another tabular dataset from scikit-learn.
- Compare RandomForestClassifier with a single DecisionTreeClassifier.
- Experiment with `n_estimators`, `max_depth`, and `max_features` to see how they impact accuracy and overfitting.
